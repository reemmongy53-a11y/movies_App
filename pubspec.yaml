name: movies_app
description: login and register screens

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  easy_localization: ^3.0.0
  provider: ^6.0.0
  flutter_svg: ^2.2.3
  carousel_slider: ^5.1.1
  http: ^1.6.0
  url_launcher: ^6.3.2
  flutter_bloc: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/translations/